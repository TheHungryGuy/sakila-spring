spring:
  application:
    name: sakila-spring
  datasource:
    url: jdbc:mysql://localhost:3306/sakila
    username: root
    password: 1hungry
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true
server:
  port: 5001

logging:
  level:
    org:
      springframework:
        web: DEBUG